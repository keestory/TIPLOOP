{
  "name": "tipping-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "티핑 네이티브 앱 셸 (Capacitor) — 배포된 웹앱을 감싼다",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
