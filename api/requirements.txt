# Vercel 서버리스 함수(api/index.py) 런타임 의존성.
# 진입점과 같은 폴더에 두어, 루트의 오케스트레이터 requirements.txt 대신 이걸 쓴다.

fastapi>=0.110
jinja2>=3.1
python-multipart>=0.0.9   # HTML 폼(Form) 파싱
psycopg[binary]>=3.1      # Supabase Postgres 접속
python-dotenv>=1.0        # 로컬 .env 로딩 (Vercel에선 환경변수 사용)
