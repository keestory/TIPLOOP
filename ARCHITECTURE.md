# ARCHITECTURE.md — 시스템 아키텍처

## 전체 구조

```
┌─────────────────────────────────────────────────────┐
│                   Orchestrator                       │
│  ┌───────────┐ ┌──────────────┐ ┌────────────────┐  │
│  │   Task     │ │   Agent      │ │   Review       │  │
│  │ Decomposer │ │   Runner     │ │   Loop         │  │
│  └─────┬─────┘ └──────┬───────┘ └───────┬────────┘  │
│        │               │                 │           │
│        ▼               ▼                 ▼           │
│  ┌─────────────────────────────────────────────┐     │
│  │              Agent Pool                      │     │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────┐ │     │
│  │  │ Code   │ │ Review │ │ Test   │ │ Doc  │ │     │
│  │  │ Agent  │ │ Agent  │ │ Agent  │ │Garde-│ │     │
│  │  │        │ │        │ │        │ │ner   │ │     │
│  │  └────────┘ └────────┘ └────────┘ └──────┘ │     │
│  └─────────────────────────────────────────────┘     │
│        │               │                 │           │
│        ▼               ▼                 ▼           │
│  ┌─────────────────────────────────────────────┐     │
│  │           Tools & Integrations               │     │
│  │  Git │ CLI │ Linters │ Tests │ Observability │     │
│  └─────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────┘
```

## 레이어드 도메인 아키텍처

각 비즈니스 도메인은 고정된 레이어를 따릅니다.
의존성은 반드시 **위에서 아래로만** 흐릅니다.

```
┌──────────────────────────────────────┐
│            Types (타입 정의)          │  ← 순수 데이터 구조
├──────────────────────────────────────┤
│            Config (설정)             │  ← 환경 설정, 상수
├──────────────────────────────────────┤
│          Providers (교차 관심사)      │  ← 인증, 텔레메트리, 기능 플래그
├──────────────────────────────────────┤
│            Repo (데이터 접근)         │  ← DB, API 클라이언트
├──────────────────────────────────────┤
│           Service (비즈니스 로직)     │  ← 핵심 도메인 로직
├──────────────────────────────────────┤
│           Runtime (실행 환경)         │  ← 서버, 큐, 스케줄러
├──────────────────────────────────────┤
│              UI (사용자 인터페이스)    │  ← 대시보드, CLI
└──────────────────────────────────────┘
```

### 의존성 규칙 (기계적으로 강제)

- Types는 어떤 레이어도 import하지 않음
- Config는 Types만 import 가능
- Providers는 Types, Config만 import 가능
- Repo는 Types, Config, Providers만 import 가능
- Service는 Repo 이하 레이어만 import 가능
- Runtime은 Service 이하 레이어만 import 가능
- UI는 모든 레이어를 import 가능 (직접 Repo 접근은 금지)

## 오케스트레이터 아키텍처

### Task Decomposer (태스크 분해기)
- 사용자 프롬프트를 분석하여 실행 가능한 서브태스크로 분해
- 각 서브태스크에 적절한 에이전트 할당
- 의존성 그래프 생성

### Agent Runner (에이전트 러너)
- 에이전트 생명주기 관리 (생성 → 실행 → 완료/실패)
- 병렬 실행 지원 (독립적 태스크)
- 컨텍스트 관리 (점진적 공개)

### Review Loop (리뷰 루프) — "Ralph Wiggum Loop"
```
CodeAgent 작업 완료
    → ReviewAgent 검토
        → 피드백 있음? → CodeAgent에 전달 → 재작업
        → 피드백 없음? → 승인 → 병합 준비
```

## 에이전트 통신

에이전트 간 통신은 **메시지 큐 패턴**을 따릅니다:

```python
# 메시지 구조
{
    "from": "code_agent",
    "to": "review_agent",
    "type": "review_request",
    "payload": {
        "task_id": "task-123",
        "files_changed": ["src/service/auth.py"],
        "summary": "인증 토큰 갱신 로직 수정"
    }
}
```

## 관측 가능성

각 에이전트 실행은 다음을 기록합니다:
- **로그**: 구조화된 JSON 로그 (에이전트 ID, 태스크 ID, 액션)
- **메트릭**: 실행 시간, 토큰 사용량, 성공/실패율
- **추적**: 태스크 분해 → 에이전트 실행 → 결과까지의 전체 트레이스

## 품질 게이트

| 게이트 | 설명 | 차단 여부 |
|--------|------|----------|
| 아키텍처 린터 | 레이어 의존성 위반 검사 | 차단 |
| 구조 검증기 | 파일/디렉토리 규칙 검사 | 차단 |
| 네이밍 린터 | 네이밍 컨벤션 검사 | 경고 |
| 테스트 커버리지 | 최소 커버리지 확인 | 차단 |
| 품질 스코어 | 도메인별 품질 등급 | 보고 |
